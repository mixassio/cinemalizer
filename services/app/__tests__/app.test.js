
const app = require('../app.js');
const db = require('../models');

describe('first test', () => {
  it('simple test',  () => {
    expect(1).toBe(1);
  });
  it('simple  2 test',  () => {
    expect(1).toBe(1);
  });
});
